import 'package:flutter/widgets.dart';

class SpacePadding{
  static const Padding minimum = Padding(padding: EdgeInsets.all(5));
  static const Padding small = Padding(padding: EdgeInsets.all(10));
  static const Padding big = Padding(padding: EdgeInsets.all(15));
  static const Padding maximum = Padding(padding: EdgeInsets.all(20));
}